## Explanation
Ran using VLLM server (check run_server.sh script)

run_experiments contains code for running the experiment studies.

RAG performing well, need to connect to augmented context from triage, diagnosis etc.

## Running
Data for RAG must be put into data/ folder

run the vllm server and then the run experiments file from the julius/ folder level to successfully run (for paths)
